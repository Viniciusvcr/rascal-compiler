program calculaAreaTriangulo;
  var base, altura: integer;
      area: integer;

  function areaTriangulo(b: integer; h: integer): integer;
    begin
      areaTriangulo := (b * h) div 2;
    end;

begin
  read(base);
  read(altura);

  area := areaTriangulo(base, altura);

  write(area);
end.
